import React from 'react'

import {CurrentPost, GridImages} from '../components/components'

const Projects = (props) => (
            <CurrentPost style={{justifyContent: 'center'}}>
             <p>Stay tuned</p>
            </CurrentPost>
)

export default Projects
